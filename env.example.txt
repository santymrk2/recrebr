# Supabase (Project Settings > API Keys — pestaña "Publishable and secret API keys")
NEXT_PUBLIC_SUPABASE_URL=https://TU-PROYECTO.supabase.co
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=sb_publishable_xxxxxxxx

# Secret key: SOLO se usa en el servidor (seed script y acciones de admin). NUNCA exponer en el cliente.
SUPABASE_SECRET_KEY=sb_secret_xxxxxxxx

# WhatsApp del negocio (sin +, sin espacios), usado para armar los links wa.me
NEXT_PUBLIC_WHATSAPP_NUMBER=5491165383672

# URL pública del sitio (usada para metadatos Open Graph)
# URL pública y canónica exacta del sitio desplegado (sin barra final).
# Es necesaria para los canonical, sitemap, robots y datos estructurados de Google.
NEXT_PUBLIC_SITE_URL=https://tudominio.com
